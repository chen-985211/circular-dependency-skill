interface:
  display_name: "Detect Circular Dependencies"
  short_description: "Find file, directory, package, and layer cycles"
  default_prompt: "Use $detecting-circular-dependencies to check whether these files, directories, packages, or architecture layers participate in circular dependencies."
