interface:
  display_name: "Detect Circular Dependencies"
  short_description: "Find cycles, boundary drift, and blast-radius bug risks"
  default_prompt: "Use $detecting-circular-dependencies to check whether these files, directories, packages, or architecture layers participate in circular dependencies, then review the highest-risk paths for boundary drift and user-visible bug risks."
